# LESSONS-INBOX · <project> · mission: <one-line goal>

<!-- Template: skills/mission/templates/LESSONS-INBOX.md → .mission/LESSONS-INBOX.md (M+, copied at init). Candidate
     CROSS-PROJECT lessons only. Test: "would this help on a different repository?" No → it is an
     R-entry in STATE.md, not a lesson.
     Written by the orchestrator (from workers' lesson_candidates and investigation conversion D5). Append-only: change
     status, never delete. Budget: ≤30 open entries (candidate|verifying). Lint: .mission/bin/memory-lint.sh
     NEVER edit the skill mid-mission. Promotion happens at retro (references/memory-and-lessons.md P8) only when all
     criteria a–h pass, checked by a fresh-context mission-verifier.
     Draft a candidate ONLY IF: root cause CONFIRMED ∧ class general ∧ (count ≥2 ∨ cost ≥1 day ∨ user-visible or
     production defect) ∧ a machine control (lint/hook/type/schema) is impossible or insufficient. Otherwise write
     "no lesson: <reason>" in the investigation. Subject = a missing mechanism, never "be more careful". -->

## Candidates

<!-- Entry format (append below, uncommented):

- L-NNN · status: <candidate|verifying|promoted(references/lessons.md#l-nnn)|rejected(<criterion or stale>)|merged-into(L-NNN)> · opened <YYYY-MM-DD>
  Lesson: <imperative, project-agnostic rule; no project names>
  Applies when: <conditions>
  Does not apply when: <conditions>
  Why not a control: <why no lint/hook/type/schema constraint prevents this class>
  Origin: <project>/<mission> · <O-NNN → F-NNN> · cost of original failure: <time lost, user impact>
  Evidence: `<command/test that fails before and passes after>` (sha <parent> → <fix>) or <F-NNN / doc URL + quote>
  Seen in: <project/mission (date)>; <second project/mission (date)> · count: <n>
  Proposed target: <references/lessons.md § <group> | SKILL.md <section> (only if the skill steered a run wrong)>
  Eval case: <evals/evals.json id <n> (drafted) | advisory: <why it cannot be a test>>
  Distiller: <agent · model @ effort> · Verifier: <agent · verdict · date | —>

-->

## Triage log
<!-- One line per curator triage or retro decision:
- <YYYY-MM-DD> · L-NNN <status change> · <reason, criterion letter> -->
