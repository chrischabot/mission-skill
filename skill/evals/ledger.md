# Eval ledger

One row per gating run, appended and never edited. `README.md` section 5 says how to fill each
column; per-case scores for the run go in `scores/<date>.tsv` in the same commit.

| date | skill commit | Claude Code | model id | judge | with drive | without drive | mean Δ | cases below 1.0 | investigations opened |
|------|--------------|-------------|----------|-------|-----------:|--------------:|-------:|-----------------|-----------------------|
