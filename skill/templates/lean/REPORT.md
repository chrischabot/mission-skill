# REPORT · <goal slug>

<!-- Written by the final drive:reviewer from the plan, the code, the test runs it made itself, and the
open items it was given, never from memory. Every line under What works names a command that passed
in this session. Anything that ran only against a fake, stub, or local stand-in says so and is never
called live. The orchestrator writes Spend from a recorded figure, or as "not measured". -->

## Outcome
<One paragraph: what was asked, what works now at which commit, and what did not get done. A run
that stopped short opens with "Stopped because".>

## What works
- <the behaviour> · `<the command that passed>`

## How to run it
```bash
<the commands, from a clean checkout>
```

## Open findings
- none

## Blocked
- none

## Learned
- <the rules this run recorded or promoted, or none>

## Spend
not measured
