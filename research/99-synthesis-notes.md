# Synthesis notes (orchestrator working file; survives compaction)

Skill target: `skills/mission/` (SKILL.md lean + references/ + templates/). Name `mission`.

## R01 orchestration (read in full)
- Control plane = files under `mission/`: BRIEF.md, PLAN.md (DAG), STATUS.md (board, gates, budget, fallback events, human queue, stop-rule log), acceptance.json (all passes:false; only verifier flips), ASSUMPTIONS.md, DECISIONS.md (ADR-lite), HANDOFF.md, CONTEXT.md (conventions/commands shared by briefs → cache friendly), loops/<id>.md, logs/, check.sh.
- Phases 0 Intake,1 Research,2 Spec,3 Design,4 Plan,5 Build,6 Verify,7 Review,8 Release,9 Retro; gate table w/ verifier per phase; gate states PENDING/PASSED/PASSED-WITH-WAIVER(DEC)/BLOCKED/PENDING-LIVE. G2 loop back to earliest invalidated phase.
- Human checkpoints only: irreversible/externally visible (deploy, data migration, push protected, publish, spend, messaging people, CI/secrets), spec sign-off L/XL (offer autonomous mode). Encode as permissions.ask/deny + PreToolUse hooks, not prose. Unattended → BLOCKED-HUMAN queue, keep working.
- Stop rules S1 success w/ evidence; S2 impossible; S3 budget caps counted in files (iter S=3,M=5,L=8, ≤20); S4 futility (no progress 2 iters, same failure signature twice, oscillation; keep best iteration); S5 guardrail (tamper); S6 escalation ladder (retry w/ feedback → change approach → tier+1 → BLOCKED); S7 never edit acceptance to pass.
- /goal verified: evaluator reads transcript only, defaults Haiku → not a gate; use conditions pointing at printed evidence + turn clause; prefer Stop hook running mission/check.sh. Outcomes (CMA) verified: rubric, grader separate context; max_iterations numbers secondary.
- Dynamic workflows verified (`/workflows`, ultracode keyword, `/deep-research`), but primitive names unverified → ask for workflow in natural language; use when >~8 parallel workers.
- Models: aliases opus→Opus 5, sonnet→Sonnet 5 on API! Pin full IDs / ANTHROPIC_DEFAULT_*_MODEL. Fable 5.1 id `claude-fable-5-1`; Opus 4.8/Sonnet 4.6 IDs unconfirmed → intake verification + documented degradation. Fable 5.1 defaults High effort in CC; Low/Medium ≈ Fable 5 cheaper → set effort explicitly. Orchestrator Fable 5.1 medium for M+; S missions Sonnet/ session model.
- Classifier: categories cyber, bio, frontier_llm, reasoning_extraction, general_harms (no chem). CC fallback to Opus 4.8 is sticky & silent → log in STATUS "Model/fallback events", reselect model at phase boundary; route exploit work to Opus 4.8 upfront; BLOCKED-SAFETY; never rephrase around.
- Headless `claude -p` w/o --bare (bare skips skills/agents/hooks); json total_cost_usd. Routines: no approval prompts, claude/ branches, self-contained prompt.
- Work breakdown: task = one verifiable change in one fresh context (≤~15 files), cites check ids, owned paths, needs DAG; fan-out ceilings S2/M4/L8/XL12; serial integration; brief template §7.2 w/ return format (STATUS/SUMMARY/FILES/CHECKS/ASSUMPTIONS/OUT-OF-SCOPE/NEXT); verifier briefs artifact+checks only.
- Session start ritual: HANDOFF → STATUS → PLAN → STATE last session → git log → smoke check. End or ~60% context → HANDOFF (reset > compaction).
- Downgrade experiment rule: run both tiers on 3 gates; downgrade only if agree 3/3.
- Size table S/M/L/XL (signals, phases, fan-out, review rounds, checkpoints, iter cap). Uncertain → larger for verification, smaller for ceremony.
- Shape rules: multi-platform → contract-first M0 + walking skeleton; bug hunt → Reproduced gate before fix; feature → codebase map, reuse conventions, flag; migration → parity inventory captured before move, strangler milestones, rollback rehearsal, parity count never decreases; website → research milestone, citation verifier, rubric+executable (links, build, Lighthouse), tournament for taste; upgrade; incident; recurring ops Routine; security-sensitive.
- R01 templates worth shipping: STATUS.md §7.1, brief §7.2, loop spec+verifier prompt §7.3, acceptance.json §7.4, DECISIONS/ASSUMPTIONS rows, HANDOFF §7.5, guardrail settings §7.6, continuation prompt §7.8, main loop §7.9. (paths: research/mission-skill/01-orchestration-control.md lines 543-851)
- arcwell evidence: gate checked only after green on clean workspace; requirements with no oracle demoted not weakened; PENDING live gates never claimed; handoff structure; 18-round review fatigue → caps + consolidated review.

## R05 frontend visual/UX (read lines 1-700)
- Layered stack: G1 deterministic (axe-core WCAG, toMatchAriaSnapshot, geometry probe JS: h-overflow, clipped text, overlap, off-viewport, target ≥24px web/44pt iOS, focus, alignment clusters, token conformance, console errors; iOS performAccessibilityAudit iOS17+ w/ justified suppressions) → G2 vision verifier on fixed screenshot matrix → G3 UX walkthrough (act via accessibility tree, screenshot each step, friction log, Nielsen heuristics, severity median of 3 Sonnet-low raters) → batched blinded human review per milestone (L/XL/consumer greenfield) generalizing arcwell m3-human-evaluation.md.
- V-MUST: design/screen-spec.md + design/tokens.md before UI code (existing product: extract tokens + reference screenshots). Verifier separate, no maker transcript; ban "looks good" (findings schema element/expected/observed/evidence box/severity/fix + checked_and_passed); image canary (transcribe title + size) → IMAGE_CHANNEL_FAILED; UI task not DONE unless G1–G3 or explicit "NOT PERFORMED: reason" in open risks. Scripted capture w/ manifest.json + env fingerprint; reference = last ACCEPTED set same env; goldens only after acceptance; never unreviewed --update-snapshots. Viewport frames + element crops (Claude downsizes 1920×1080→1456×819). Loop bound 5 iter/screen (8 hero), keep best iteration. Capability probe + fallback order; degrade to "not performed".
- Rubric 0–4 on 10 craft criteria (hierarchy, layout_integrity, spacing_rhythm, alignment, typography, color_contrast, system_consistency, microcopy, states_coverage, platform_conventions) + originality via tournament; Gate A accessibility floor binary. Thresholds S/M: all ≥2, mean ≥3.0; L/XL: 1,2,6,9 ≥3, mean ≥3.3. AI-default tells checklist (frontend-design skill). Tournament: pairwise, position-swapped, disagree=tie.
- Models: design planner Opus 4.8 high; UI maker Sonnet 4.6 medium; capture runner/G1 triage Sonnet low; per-screen vision verifier Opus 4.8 medium (NOT Sonnet low; if forced, Opus re-verify); regression screener Sonnet medium w/ 1-in-5 Opus recheck; walkthrough Sonnet medium; heuristic reviewer Opus medium; tournament judge Opus high (Fable high XL brand); milestone design review Opus high / Fable medium XL. Sonnet 4.6 computer use only via older beta tool.
- Tooling verified: Playwright MCP (CLI+skills more token efficient), Chrome DevTools MCP (lighthouse_audit), ios-simulator-mcp (≥1.3.3), XcodeBuildMCP, simctl io screenshot, status_bar override; images to subagents as files opened with Read.
- Dirs: design/{screen-spec.md,tokens.md,references/}, verification/{matrix.yaml,screens/<run>/,reports/,accepted/,walkthroughs/,human-review/<milestone>/}.
- Scope table S/M/L/XL matrix sizes; shape rules (bug: failing deterministic check first; feature: extract tokens, regression screener; migration: off unless admin UI; website: tells checklist, link/line length/Lighthouse; i18n, design system, a11y remediation, email/PDF).
- Templates: matrix.yaml web/iOS §7.1, verifier prompt+schema §7.2, rubric §7.3, G1 checklist + geometry-probe.js §7.4, walkthrough §7.5, human batch protocol §7.6, report §7.7 (file lines 226-637).

## R07 memory (read §1-2, §4-7.7)
- CONFLICT w/ R01 dir: R07 `.mission/` vs R01 `mission/`. Decide one (→ `.mission/`? visible better? DECIDE at synthesis).
- One home per fact kind: CLAUDE.md (commands/conventions; mission only PROPOSES edits after ≥2 violations); STATUS.md = what's done (Goal verbatim direction, done-means numbered gate commands, Progress from gate results only, Board, risks, done log); STATE.md = what's true (Resume runnable pointer, Rules index, Verified facts F- w/ Evidence cmd+output line, Level local-run|ci|staging|production|doc-source, verified & recheck-by dates; Hypotheses H- w/ discriminating check, FALSIFIED kept; General rules R- imperative + Applies when + Instances ≥1 F + Counter-cases, conf low=1/med=2/high≥3; Open failures O- symptom/repro/observed/expected, CONTRADICTION; Decisions index; Last session keep 5). DECISIONS.md append-only supersede; RESEARCH.md S- entries (URL, accessed, reliability primary|secondary|vendor-claim|unverified, recheck-by, supports); LESSONS-INBOX.md L-; investigations/; archive/; tmp/ gitignored; config.
- Only orchestrator writes STATE/STATUS; workers return `memory_delta` block (§7.8 lines 640-660); ID-level edits never whole-file rewrite (ACE context collapse).
- Budgets: STATE ≤150 lines, STATUS ≤100, inbox ≤30, lessons.md ≤40 entries/300 lines, loaded hook output ≤~3k tokens. Curator pass §7.10 (lines 807-839) w/ information-loss verifier.
- Hooks: SessionStart matcher startup|resume|compact prints Resume+Rules index; Stop hook freshness check respects stop_hook_active (CC overrides after 8 blocks), opt-in enforce for headless; PreCompact snapshot; SubagentStop XL. Scripts §7.9 lines 661-806 (untested; hook input fields unverified).
- Skill compounding: inbox during work, never edit skill mid-mission; promote at retro only if (a) root cause verified (b) ≥2 missions or 1 costly incident (c) general + Applies/Does-not-apply (d) eval case in evals/evals.json skill-creator format (e) independent verifier (f) non-duplicative (g) budget (h) SKILL.md body edited only when skill steered run wrong (Anthropic /verify reversal). Personal skills don't load in cloud/Routines → vendor into repo .claude/skills.
- CL-Bench is Berkeley (arXiv 2606.05661), 5-stage framing is Lance Martin's X article; "naive ICL outperforms dedicated memory systems" → plain files. Martin: "task-specific memory instructions are needed" → templates carry discipline for Sonnet.
- Models: loader/lint no model; investigator Sonnet high→Opus high after 2 falsified; evidence verifier Sonnet low; distiller Opus high (Fable XL cross-mission retro); promotion verifier Sonnet high fresh; curator Sonnet medium + info-loss verifier Sonnet high.
- Scope: S no .mission (PR description + 1 inbox line to ~/.claude/mission-lessons-inbox.md); M STATE+STATUS+INBOX, SessionStart hook; L full + Stop + PreCompact; XL state/*.md split, SubagentStop, weekly retro.
- Shape: multi-platform per-platform fact sections, contract SoT path not duplicated; bug hunt Open failures spine, falsified hypotheses kept, lesson as detection technique; feature read CLAUDE.md don't copy; migration `## Invariants` w/ parity evidence on both paths, cutover evidence level staging/production; website RESEARCH primary, public claims trace to S-ids.
- Templates: dir layout §7.1, STATE §7.2, STATUS §7.3, DECISIONS §7.4, RESEARCH §7.5, lesson/promotion/eval §7.6, session protocol §7.7 (lines 398-638), prompts §7.11 (840-897).

## R04 testing (read §4-6)
- Traceability: TEST-PLAN.md (M+) criteria IDs → gate level, oracle type, location; tests carry `@req:AC-###`; verifier computes coverage from code; DONE = every must criterion ≥1 passing oracle in verifier's run; skipped/quarantined/flaky → UNPROVEN. Broad E2E not sole oracle for >3 criteria. Traceability lint L/XL.
- Anti-bloat B-1..8: budget 1 specific oracle + ≤2 edge cases per criterion (property test for large input space); ban tautological/mock-own-code tests; mock only third parties behind a port; property models from spec; bloat audit; lowest level that observes requirement; delete redundant w/ record; coverage reported never gated.
- Who writes: acceptance tests by separate test-author agent before impl, red on pre-change tree, committed & frozen; implementer unit tests don't count unless ID + audit; verifier re-executes every gate in clean worktree; TEST-DISPUTE instead of editing.
- Anti-cheat G-1..8: frozen paths (tests/acceptance, regression, golden, configs incl. playwright/vitest/stryker, CI, .claude/hooks, settings); 3 layers deny rules + PreToolUse hook (Edit|Write|MultiEdit|Bash) + hash manifest checked by verifier/CI; verbatim stop line "If a test appears wrong... STOP and file TEST-DISPUTE. Do NOT modify, skip, delete, or special-case tests, and do NOT carve out the code"; test-diff audit (deleted tests, .only/.skip/XCTSkip/#[ignore], weakened assertions, goldens changed, NODE_ENV test branches, fixture literals in prod, passWithNoTests, count decrease) w/ grep pre-pass; forbidOnly; targeted mutation L/XL (Stryker incremental, cargo mutants --in-diff) survivors block; oracle-bite S/M (deliberate break shows test fails); test-only diffs → full audit. Held-out 1–3 verifier scenarios per epic L/XL or after cheating.
- Backend: offline credential-free runs w/ network guard; Workers Vitest integration `@cloudflare/vitest-plugin` (renamed from vitest-pool-workers Aug 2026) local D1/KV/R2/DO; deterministic fakes + oracle-bite per adapter; contract goldens; failure injection asserting bounded side effects; security tests; complexity-based perf budgets; live canaries separate, spend ceiling, cleanup read-back, PENDING until evidence.
- E2E: golden journeys 3–7/surface L, ≤12 XL; seeded isolated data, assert outcomes; Playwright forbidOnly, retries 2 CI only, trace on-first-retry; iOS xcodebuild test, erase simulator, launchArguments seed; flake policy (record, quarantine expiry ≤2 stages, retries never >2); API E2E vs wrangler dev.
- VERIFICATION.md (verifier output; commands, exit codes, counts, red-before-green, quality proof, audit, criteria matrix, verdict PASS|FAIL|UNPROVEN).
- Models: test strategist Opus high L/XL / Sonnet high S/M; test author Sonnet high (Opus high for concurrency/security/money/parity); gate runner Sonnet low; test-diff auditor Sonnet medium S/M, Opus medium L/XL/frozen touched; mutation triage Opus medium; flake investigator Sonnet high→Opus; LLM eval grader Sonnet low/medium calibrated ≥20 cases; final grader Opus high L/XL / Sonnet high S/M; Fable only orchestrator sign-off reading VERIFICATION.md.
- Gate table per shape×scope §6.2 (lines 478-503) — paste-ready. IF/THEN §6.3: S = 1 failing-first test + suite + diff audit; bug → regression protocol, repro frozen, stress ≥1 in N, fix passes ≥10N; feature → characterize touched behavior + criteria tests + 1–3 journeys asserting seeded aggregates; migration → parity protocol, no behavior change in same PR; greenfield multi-platform → shared contract fixtures both consumers, XCUITest vs wrangler dev; website → build, link check, Lighthouse CI, journeys, claims → research log; AI → eval ≥20 tasks N≥3 trials; auth/payments/state machines → model-based property + mutation; unavailable gate → UNPROVEN never mock-substitute; perf/upgrade/infra rules.
- Templates: TEST-PLAN §7.1, protected settings §7.2, protect-frozen.sh §7.3, TEST-DISPUTE §7.4, test-diff audit §7.5, VERIFICATION.md §7.6, bug regression §7.7, migration parity §7.8, prompts §7.9, configs §7.10 (lines 548-968). Hook exit-code semantics unverified → setup self-test.

## R06 verification/adversarial review (read §3.4-5)
- arcwell data: 18 M1 rounds, new-defect counts never trended down (14,11,7,13,15,15,13,13,7,8,15,15,18,13,19,17); closed by owner decision; fixes are main late defect source; recurring classes (delimiter identity, cursor witness) → design problem; late rounds about instruments (non-causal mutation targets, overstated registrations); honesty machinery worked (parent not FIXED while sub open; residuals not claimed closed; goalpost-moving judged; CONTESTED w/ evidence); reviewer lacking test files → false findings; consolidated review at integration found cross-surface defects. "Zero findings" unreachable stop condition.
- V1–V8: only verifier flips gates; verifier brief = artifact locations + criteria/rubric + evidence bundle (incl. tests + gate outputs) + cited spec excerpts, NEVER maker transcript/self-assessment (git diff --stat OK); read-only (tools allow-list / read-only worktree / git status clean check); re-run or observe evidence; three-valued PASS/FAIL/UNVERIFIED (UNVERIFIED → gate PENDING); verifier tier ≥ maker for blocker-capable gates; contract criteria before build (loops/<id>.md); maker self-check recorded never satisfies V1.
- R1–R7: review types only by trigger table (§7.3); one lens per reviewer, ≤~400 changed LOC or one surface; told clean report valid, inventing findings = failure; every candidate blocker/major → refutation pass by separate agent (CONFIRMED/REFUTED/UNVERIFIED) — mirrors Claude Code Review; panels 2–4 lenses L/XL/integration points; pre-existing never blocks; security on Opus 4.8.
- F1–F6: finding schema (id, severity, lens, file:line, claim, evidence repro/construction, expected/observed, impact, fix class); severity blocker/major/minor/nit + pre-existing flag + question type; dispositions FIXED (commit + test failing w/o fix) / REFUTED / RESIDUAL / DEFERRED (not goalpost-moving) / CONTESTED; verify FIXED against original construction.
- C1–C7 convergence: close when round yields no CONFIRMED blocker/major; caps S=1,M=2,L=3,XL=4; re-review = remediation diff + callers + original constructions; same class twice → design review; >50% instrument findings from round 3 → bounded instrument audit; heavy panels at integration points, automated gates every milestone; escalate tier only on CONTESTED or escapes.
- G1–G4 graders binary w/ evidence; scores only taste w/ anchors; start from FAIL; calibrate 3 good + 3 bad; verdict derived mechanically. S1–S3 refusal → discard partial, log, re-run Opus 4.8, UNVERIFIED meanwhile.
- Models: checklist verifier Sonnet low w/ seeded-defect canary ~1 in 10 → miss escalates to medium; loop grader Sonnet medium, every 5th PASS re-graded Opus medium; refuter Sonnet high (Opus medium concurrency/auth/data), 1-in-3 blocker refutations sampled; correctness lens Opus medium (high integration, Fable medium XL consolidated); test-quality lens Sonnet high; spec review Opus high (Fable medium XL); design review Opus high; security Opus high never Fable; perf Sonnet high; UX per R05; fact-check Sonnet medium (Opus medium public claims); release readiness Opus medium; dedupe Sonnet low; CONTESTED adjudicator tier-up Opus high/Fable high.
- Templates: gate verifier prompt §7.1, refutation prompt §7.2, trigger table + lens blocks + personas §7.3, finding YAML §7.4, disposition log §7.5, convergence block §7.6, grader rubric §7.7, custom agent files §7.8 (lines 439-802).

## R02 model routing (read §3.4-7.2)
- Pricing verified: Fable 5.1 $10/$50, cache read $0.25; Opus 4.8 $5/$25 read $0.50; Sonnet 4.6 $3/$15 read $0.30. Post "5×" wrong (2× fresh, 0.5× cache reads). Newer tokenizer ~30% more tokens (O/F). Warm-cache Fable orchestrator turn ≈ Opus cost.
- IDs: `claude-fable-5-1`, `claude-opus-4-8`, `claude-sonnet-4-6`. Aliases forbidden (opus→Opus 5, sonnet→Sonnet 5 on API). Sonnet 4.6 active until ≥Feb 2027, Opus 4.8 ≥May 2027; Sonnet 5 $2/$10 exists — mention to user, single table override point.
- Effort ONLY settable in agent frontmatter `effort: low|medium|high|xhigh|max` (issue #72596 not planned); Agent tool can pass `model` per call but not effort → ship agent files per role@effort, naming `<role>-<model>-<effort>`. Dynamic workflow agent() accepts opts.effort.
- Preflight PF1: CLAUDE_CODE_SUBAGENT_MODEL unset; CC version (Fable 5.1 ≥v2.1.257, Opus 4.8 ≥v2.1.154); probe agent per model ID confirming answering model (allowlist silently inherits). Ship project `Explore` agent pinned Sonnet 4.6 low (built-in Explore inherits capped at Opus → Opus 5). PF2 degradation: no Fable → orchestrator/planner Opus xhigh; no Opus → Sonnet high + human review risky; no Sonnet 4.6 → stop and ask. PF3 no per-agent models → separate `claude -p --model` sessions.
- Cache: switching model/effort invalidates cache (except Fable 5.1 effort changes); MCP connect/plugin toggles invalidate; skill loads append; brief order: role preamble → stable pointers → task → output format; return payload ≤500 words.
- Escalation ladder ES1: S/medium → S/high (fresh ctx + ≤300-word failure summary + diff) → O/high → F/high → human; 2 failed attempts per rung; ES4 skip to O/high for auth, payments, data migrations, concurrency, security boundaries, public API contracts, infra/CI; Fable max 2 attempts w/ written reason; de-escalate after 5 first-pass + clean audit; promote role after 2 audit disagreements; context exhaustion/format miss = failed attempt. Escalate by spawning agent, never switching orchestrator /model.
- Guards GD: deterministic first; grader differs in model from Sonnet maker (min Opus medium when no deterministic oracle); Sonnet low only for checklists over deterministic evidence; sampled audit ≥10% (1–5) accepted cheap outputs by Opus high blind; track first-pass rate <60% → promote; classification cascade; adversarial reviewer different model than maker.
- CF classifier: pre-route exploit/pentest/malware/offensive to Opus 4.8; discovery on own code OK; log fallback events; reassert model at phase boundary; refused worker → Opus high same brief; second refusal → human; never rewrite brief; bio may reroute to Opus 5 → ask human.
- Budget CB: BUDGET.md (or STATUS section for S) soft/hard caps; ledger row per spawn; phase boundary review, >50% over allocation → re-plan; soft cap pause+decision, hard cap stop, `--max-budget-usd` headless; marginal-value stop 2 iterations no newly green gate/verified finding; subscription → plan-usage %. Caps S $8/$20, M $50/$120, L $300/$700, XL $1500/$3500; concurrency S3 M5 L8 XL15; phase allocation plan/spec/arch 15, research 10, impl 45, verify/review 20, reserve 10.
- Role matrix 20 rows (§5 lines 413-435). R02 orchestrator F/high; R01 says F/medium (5.1 low/medium ≈ Fable 5 cheaper). DECIDE: F/medium default, high for intake/spec/design/adjudication (effort switch keeps cache on 5.1).
- Profiles: Standard / Lean (Opus orchestrator) / Degraded.
- Agent frontmatter examples §7.2 (lines 525-634); BUDGET.md §7.3; escalation handoff brief §7.4; sampled audit prompt §7.5; phase cost review §7.6; preflight §7.7.

## R03 spec & design (read §4-6)
- Scope classifier SPEC-R01: ambiguity A0/A1/A2 × blast radius B0 one file/behavior, B1 one component, B2 multiple components/public surface, B3 multi-platform/data migration/money/irreversible. XL if B3 or (A2∧B2); L if max=2; M if max=1; else S. Record w/ justification; revise upward anytime, downward only w/ recorded reason after impl starts.
- Artifacts by class: S task card (goal, repro/acceptance, oracle, constraints), 0 review; M charter-lite + mini spec flat IDs + hand YAML registry, 1 review round; L charter + spec domain IDs + designs + ADRs + registry, 2 rounds lens panel; XL + milestone gates + generated registry + CI validator + STOP list + migration plan, 2 (+1 for new-section blockers) + consolidated cross-milestone review. (Spec review caps separate from impl review caps.)
- Intake: explore first (brownfield read-only repo map; novel domain web research); every gap = assumption A-nn (statement, reversal cost, verification); ask human ONLY high-reversal-cost unknowns, ≤3 multiple-choice Qs w/ recommended default per round, ≤2 rounds (platforms, data retention, spend ceilings, irreversible external actions, taste anchors/reference apps, what must not change); headless → defaults marked ASSUMED (unconfirmed). Charter: goals, non-goals, users, constraints, success metrics w/ measurement method, assumptions, risks, STOP conditions, class, DoD split product/engineering/operational.
- Spec: IDs `<DOMAIN>-<NNN>`, tests `<DOMAIN>-T<NN>`, outcomes OUT-NNN, never renumber, withdrawn kept; EARS; every external boundary ≥1 unwanted-behavior requirement; each requirement names oracle kind (unit|property|contract|integration|scenario|e2e|visual|a11y|live|human) + falsifiable criterion, no unmeasurable adjectives; P1 journeys Given/When/Then; [NEEDS CLARIFICATION] markers block P1; registry validated by script; spec-anchored (behavior change updates spec+registry same change set).
- Design: ≥2 alternatives else it's a task list; ADR (Nygard) for irreversible choices; backend lists platform limits w/ sources (D1 10GB/single-threaded), single owner per entity/side effect, lifecycles as state machines w/ illegal transitions, idempotency/retry semantics; frontend IA/screen inventory, P1 flows, tokens, per-screen spec w/ state matrix + layout assertions; HIG / WCAG 2.2 AA; ≤~20 pages else split.
- Review: fresh-context reviewer w/ rubric R1–R12 (§7.7), no transcript; findings cite ID + failure scenario; same disposition vocab as R06; after gate, implementation in fresh context loaded from files.
- Models: scope classifier Sonnet low/medium + keyword force-reclassify; discovery sweeper Sonnet medium (spot-check 3 pointers); charter author Opus medium M / Fable high L/XL; spec synthesizer Opus medium M / Opus high L / Fable high XL; backend & frontend designer Opus medium/high; template filler Sonnet low/medium; registry validator script; spec reviewer Opus medium/high (XL one lens Fable medium when Opus-authored); security lens Opus.
- Shape: greenfield ≥L, API contract own domain w/ schema file both sides test; Cloudflare limits table + store ownership; SwiftUI HIG, Dynamic Type, Light/Dark + largest AX size in state matrix; user photos → retention/deletion + privacy section; taste → ask for 2–3 reference apps, tokens before first screen. Bug: bugfix spec Current/Expected/Unchanged + repro; no repro → repro first deliverable; public contract change → L. Feature: discovery → conventions digest; extend existing ID scheme; screen specs only for new/changed; dashboard metrics definition requirement (source, formula, tz, refresh, empty/partial). Migration ≥L: plan skeleton, behavior inventory keep/drop/change, shadow/replay parity before cutover, disable legacy owner before enabling new, security lens if secrets move. Website: research questions, claims bind to source+date, content strategy, sitemap, page inventory, page-template screen specs, messaging hierarchy, docs synthetic tree test ≤3 clicks, STOP unsourced comparative claims. Spike: question + timebox → ADR. Perf: numeric metrics + harness before change. Docs-only ≤M.
- Templates §7.1 charter (+task card), §7.2 spec skeleton + bugfix variant, §7.3 registry YAML schema, §7.4 backend design + ADR, §7.5 frontend + screen spec, §7.6 migration plan + website brief, §7.7 spec review rubric, §7.8 intake/discovery prompts (lines 549-1017).
- WARNING: edit strings containing `$\``, `$'`, `$&` corrupt files in edit_file/apply_changes. Avoid.

## R09 swarms/workflows (read §3-6)
- Patterns table: pipeline (chaining), classify-and-act (routing), fan-out/map-reduce, adversarial verification, loop-until-done, best-of-N, tournament — preconditions, cost, failures. Default per-item pipelines; barriers only for dedup/rank/synthesis. Workflows fix agentic laziness, self-preference, goal drift. Swarm = budget decision (tokens explain 80% variance).
- Reconcile Cognition/Anthropic: parallel readers + single writer; parallel writers only on pre-decided partitioned surfaces w/ machine-verifiable success (Carlini C compiler: 16 agents collide on same bug → re-partition via oracle); taste → N proposals → 1 writer. No-swarm: coupled edits, shared schema/migration, shared runtime state w/o per-lane instances, tiny tasks (<~15min/≤3 files), unclear requirements.
- Surfaces verified: subagents (.claude/agents fields incl model, effort, isolation, permissionMode, tools, maxTurns, skills, memory, background, hooks; `tools: Agent(worker,...)`); dynamic workflows (Workflow tool, 16 concurrent, 1000/run, clean context per agent, per-call model/effort, /workflows pause/stop/save as command, acceptEdits so pre-allowlist); agent teams experimental → don't depend; /batch (5–30 worktree subagents → PRs); /deep-research; `/effort ultracode`. Headless: -p 10-min idle ceiling for background agents; `--worktree` → .claude/worktrees/<name> branch worktree-<name>, `.worktreeinclude`; -p runs leave worktree locks; bug #47548 spawn isolation from primary checkout only. Worktrees don't isolate ports/DB/simulators.
- SW rules: go/no-go checklist before >2 agents recorded in STATUS; freeze CONTRACTS.md (interfaces, naming, tokens, error conventions) before parallel writing, lane returns BLOCKED(contract-change); stop adding agents when ≥2 lanes hit same root cause; surface by scale (≤5 lanes Agent calls; ≥10 homogeneous → workflow; CI/overnight → claude -p script); degrade workflow → parallel Agent → sequential → -p script → single session; worktree + owns globs disjoint checked by script; branch `claude/<mission>/<lane>-<slug>`, artifacts `<mission dir>/lanes/<lane-id>/`; per-lane ports/DB/simulator; one integrator merges serially onto integration branch, reruns lane checks + smoke, revert on failure; verifier on merged branch; conflict resolver ≠ maker; cleanup worktrees; lanes checkpoint incrementally (progress.md, resume), JSON return ≤400 words; stall rule retry once narrower → tier up → STALLED; never large artifact in one tool call; DAG waves, critical path strongest; writers/wave S2 M4 L8 XL12, readers ≤16 in workflow; split lanes >1 context or >15 files; stop new waves at >80% budget.
- Models: swarm planner Opus high S/M, Fable high L/XL; maker lanes Sonnet medium → Opus high after one failed retry; mechanical Sonnet low w/ oracle + 10% sample; reader Sonnet medium; per-item verifier Sonnet high different lens (Opus high security/concurrency/migration or when maker Opus) w/ 1-in-10 PASS Opus audit L/XL; merged verifier Opus high (Fable XL release); integrator Opus medium; classifier Sonnet low w/ 5% double-classify; tournament judge Opus medium; synthesizer Opus high; babysitter script. Lanes never Fable by default.
- Shapes: multi-platform freeze contract + stub generation lane, parallel backend/iOS lanes disjoint + contract-test lane, per-lane simulator; design proposals tournament → 1 writer. Bug: 3–5 read-only hypothesis lanes → collapse to one writer; flake lanes need own runtime. Feature: spine lane (shared layout/routing/state) merges first then widget lanes ≤4; codebase reader writes CONVENTIONS.md. Migration: workflow discover → per-module migrate → verify → barrier; interface change + shim serial first; differential oracle partition by endpoint; one migration writer. Website: map-reduce research 3–6 angles + synthesizer + skeptic; content lanes vs frozen voice guide; spine shell lane; naming generate ≥10 → tournament top 4. Audit fan-out readers; flaky test backlog good for parallel writers w/ lock claims; infra/CI no swarm.
- Templates: §7.1 pattern table, §7.2 go/no-go, §7.3 agent files, §7.4 worktree+merge protocol w/ overlap script, §7.5 lane contract + return JSON, §7.6 swarm STATUS section, §7.7a workflow script, §7.7b run-wave.sh (lines 223-627).

## R08 failure investigation (read §4-6)
- DBG intake: failure record before touching code (symptom verbatim, expected/observed, first seen, env fingerprint, blast radius, detection); mitigation ≠ resolution; "check the plug" checklist (build current, right binary/deploy, env/bindings, test actually runs); classify against hard-bug-class table (§7.7).
- Reproduce: repro script/test exits non-zero + one-line signature; REPRODUCED gate before fix; baseline failure rate ≥5 failures or deterministic; minimize (>50 lines/60s); UNREPRODUCED(reason) only for unreachable envs → observational + speculative fix flagged; freeze repro (protected path, TEST-DISPUTE).
- Isolate before theorizing: git bisect run, ddmin input/config, differential env runs (local vs preview, simulator vs device, old vs new dep); instrument boundaries once + Heisenberg check; ≤200 log lines in orchestrator context.
- Hypothesis ledger: statement, mechanism, prediction, discriminating experiment, expected under true/false, raw result, status; one variable per experiment; FALSIFIED kept; ≥3 hypotheses across layers; CONFIRMED only by two-way intervention by separate confirmer; 3 failed fixes → stop, "question the design" escalation.
- Swarm optional only if REPRODUCED + ≥3 independent OPEN hypotheses each ≥10min + budget; one hypothesis per lane in own worktree running an experiment (read-only theory lanes just produce stories); confirmer on leading SUPPORTED.
- Proof PRF: red→green by verifier in clean worktree; intermittent n ≥ ln(α)/ln(1−p) consecutive clean runs w/ state reset (≈3/p for 95%, 4.6/p 99%; α 0.01 for money/auth/data/concurrency) — CONFLICT w/ R04 10N; DECIDE: use formula (with 2× margin when state can't reset). Fix touches confirmed mechanism (no catch-ignore/retry/timeout bump unless cause external transient); test-diff audit; sibling sweep (grep/AST pattern, every hit dispositioned); masked-bug check removing old workarounds; deployed → FIXED-PENDING-LIVE until recurrence check.
- Lessons LSN: conversion step → regression test + optional control (lint/hook/type/schema) + generic audit task (enumerable scope + completion check) + lesson candidate; prefer controls over prose; scoped + evidence; blameless (subject = missing mechanism).
- Process failures PFX: 13-class taxonomy §7.6 (premature done, gamed tests, lost context, wrong assumption, tool misuse, spec gap, classifier block, missing env, …) w/ detection signature, remedy, mechanical control; recurs twice → mechanical control. Escalation ESC-02: 2 rounds no SUPPORTED, 3 failed fixes, confirmer refutes twice, budget exceeded. ESC-03 ask human: destructive/prod/money experiments, needs creds/devices, fix changes public contract/product behavior, persistent refusal, Fable exhausted.
- Ladder: rung 0 scripts → 1 Sonnet high (S deterministic) → 2 Opus high (default M+; start here for concurrency, memory, auth, payments, data integrity, platform runtime, toolchain) → 3 Fable high hypothesis-space reset (compact ledger input, max 2) → 4 human. Roles: triage Sonnet medium; plug runner Sonnet low; repro builder Sonnet high/Opus medium races; log extractor Sonnet low quoting verbatim; confirmer Opus medium/high; fixer Sonnet high / Opus high cross-component; proof runner script; sweeper Sonnet medium w/ Opus blind sample ≥3; distiller Opus high.
- Scope: S inline record in PR, one grep sweep; M ledger file; L + differentials + live recurrence + postmortem; XL mitigate first, O-id per cause, incident drill row. Repro budget S 30min, M 2h, L half day → else observability deliverable. Shape: migration differential replay first, env/config drift first hypothesis; feature bisect against branch base; pre-existing never blocks; website S-depth, conversion target is a check; upgrade lockfile bisection; perf benchmark threshold bisect ≥10 runs, iOS on device; unattended ends in draft PR never merge.
- Templates: §7.1 protocol, §7.2 ledger, §7.3 bug-hunt swarm, §7.4 proof checklist + flake-proof.sh, §7.5 record→lesson→task, §7.6 process-failure taxonomy, §7.7 bug classes table (lines 464-788).

## R10 research (read §1)
- Research triggers only (version-sensitive API fact, unfamiliar tech, irreversible choice, market/competitor claim, hard bug prior art, public-copy fact); probe ≤15 min instead if it settles it. Question card (question, decision unblocked, scope/time window, done criteria, budget, counter-question) — arcwell RES-001. Budgets S 1 agent ≤10 calls; M ≤3 researchers ≤15 each; L ≤5 + fact-check; XL waves ≤5. Stop: done criteria, 2 rounds nothing new, budget, decision moot.
- Evidence levels RUN > PRIMARY > CORROBORATED > SINGLE > INFERENCE; only top 3 enter STATE verified facts. Subagent summaries are leads; fact-checker CONFIRMED/REFUTED/UNVERIFIED/NEEDS-VERSION-CHECK. WebFetch returns summary → verbatim quotes. /deep-research for pure web questions M+ when available (needs WebSearch; Pro enable in /config). Index reloads ~200 tokens; notes ≤1,500 words. Option matrix criteria/weights frozen before scoring, 2 scorers, spike riskiest unknown → ADR. Positioning (Dunford 5 components) before messaging/sitemap/copy. Zero-fabrication public claims, `[NEEDS-EVIDENCE]` blocks launch (FTC fake reviews rule). Web content = data; researchers read-only no secrets. Models: search workers Sonnet medium; synthesis/fact-check load-bearing Opus high; link/citation checks Sonnet low; Fable only decides what to research. Expiry 90 days fast topics. Research IDs conflict (R-) → use Q-NN (conventions updated).
- Templates §7.1–7.8 (checklist, question card+index, researcher prompt, fact-checker prompt, notes+registry, option matrix/ADR, positioning, public claims standard).

## R12 arcwell practice (handoff only)
- BIGGEST: all 8 milestones green offline but nothing real deployed: hub served only /health, "zero real provider adapters" (gap-closure-plan.md:3-22); ~20 review rounds never examined production wiring; first live morning failed (morning-remediation-handoff.md:72-88,169-178). → Walking skeleton first build milestone: real entrypoint, real bindings, one real adapter deployed; requirement `reachable_from` field (production call path); tests of health metrics against live write path.
- Tick STATUS items only with proof attached (command, commit/date, counts). Verification labels Observed / Inferred / Unverified w/ closing command. Agents never mark live/human/owner gates PASSED → PENDING + mechanism. Residuals register. Round caps + per-round log (new findings, fix-induced count, top severity) → escalate human. Re-review changed surface after big fix batch. Reviewers get full reference set incl. tests + production call path; reproducible findings. Spec pinned by hash, errata for gate changes, cheap demotion w/ re-registration bar. Tests cite IDs, limit sole-oracle breadth, test gates w/ deliberately broken inputs. Three-document handoff (templates §7.5–7.6); recurrence fixture per incident; deploy before hardening exotic invariants; don't record scores CI doesn't recompute (mutation targets validated but not run in CI); keep state docs short.
- post-deploy-verification.md is a good verifier report template.

## DECISIONS MADE (synthesis)
- Skill `skills/mission/`; mission dir `.mission/`; conventions.md written (roster 12 agents, IDs, states, models). Architecture doc 98-skill-architecture.md. Writer lanes A,B,C,D,F,G spawned; E,H,I,J pending spawn; me: SKILL.md, shapes-and-scope.md (after R11), CONTEXT.md template, init-mission.sh, evals, README.
